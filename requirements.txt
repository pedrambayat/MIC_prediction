# Dependencies for the MIC prediction project.
#
# Versions are lower bounds reflecting the Google Colab environment the work was
# originally run in (Python 3.10+, matplotlib 3.10). They are not pinned to exact
# versions because the original run predates this file — treat them as a starting
# point, not a reproduced lockfile.
#
#     pip install -r requirements.txt

# --- Core modeling (notebook 06) ---
torch>=2.0
scikit-learn>=1.3
xgboost>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10

# --- Peptide descriptors ---
# modlAMP provides the physicochemical features (charge, hydrophobic ratio,
# instability index, Bowman index, ...). It can be fussy to install; if it fails,
# notebook 06 still runs from the pre-computed baseline.csv on HuggingFace.
modlamp>=4.3

# --- Data access ---
huggingface-hub>=0.20
requests>=2.31
openpyxl>=3.1          # reading the extinct-peptide .xlsx

# --- Plotting ---
matplotlib>=3.7
seaborn>=0.13

# --- Structure parsing (notebooks 04-05) ---
# Also requires the DSSP binary, which is NOT a Python package:
#   Ubuntu/Debian:  sudo apt-get install dssp
#   macOS:          brew install brewsci/bio/dssp
biopython>=1.81

# --- Notebook environment ---
jupyterlab>=4.0
ipykernel>=6.25

# Notebook 03 (ColabFold / AlphaFold2 batch folding) is not covered here — it
# installs its own dependencies and must be run in Google Colab.
